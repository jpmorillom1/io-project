server:
  port: 8080

spring:
  application:
    name: io-api

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/io_platform}
    username: ${DB_USER:io_user}
    password: ${DB_PASSWORD:io_pass}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  web:
    resources:
      add-mappings: false
#  mvc:
#    throw-exception-if-no-handler-found: true

# ── LangChain4j ────────────────────────────────────────────────────────────────
# Chat model: OpenAI-compatible apuntado a Groq
# Embeddings: AllMiniLM local (no requiere API key)
langchain4j:
  open-ai:
    chat-model:
      api-key: ${GROQ_API_KEY}
      base-url: https://api.groq.com/openai/v1
      model-name: ${GROQ_MODEL_NAME:llama-3.3-70b-versatile}
      temperature: 0.7
      max-tokens: 2048
      log-requests: true
      log-responses: true

# ── Configuración propia de la app ─────────────────────────────────────────────
app:
  chroma-url: ${CHROMA_URL:http://localhost:8000}

# ── Logging ────────────────────────────────────────────────────────────────────
logging:
  level:
    jpap.dev: DEBUG
    dev.langchain4j: INFO
    org.hibernate.SQL: WARN
